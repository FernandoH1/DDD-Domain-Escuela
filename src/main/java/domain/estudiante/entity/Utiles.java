package domain.estudiante.entity;

import co.com.sofka.domain.generic.Entity;
import domain.generic.UtilesId;

public class Utiles extends Entity<UtilesId> {

    public Utiles(UtilesId entityId) {
        super(entityId);
    }
}